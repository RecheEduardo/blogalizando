<div class="container fadeInDown">
  <header class="border-bottom lh-1 py-3">
    <div class="row flex-nowrap justify-content-between align-items-center">
      <% if policy(:category).index? %>
        <!-- Feature do pundit que apenas fornece acesso as categorias
            se o usuário tiver acesso ao index (se admin for true)  -->
        <div class="col-4 pt-1">
          <%= link_to t('app.menu.categories'), categories_path, class:'link-secondary'%>
        </div>
      <% end %>
      <div class="col-4 text-center">
        <%= link_to t('app.title'), root_path, class:'blog-header-logo text-body-emphasis fw-bold', style:'text-decoration: underline;' %>
      </div>
      <div class="col-4 d-flex justify-content-end align-items-center">
        <%= render 'layouts/links_account'%>
      </div>
    </div>
  </header>

  <%= yield :category_filter %>
</div>