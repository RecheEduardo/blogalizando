<%= render 'longer_featured'%>

<div class="row mb-2">
  <%= render 'medium_featured'%>
  <%= render 'medium_featured'%>
</div>

<div class="row g-5">
  <div class="col-md-8">
    <h3 class="pb-4 mb-4 fst-italic border-bottom">
      All my journey
    </h3>
    <%= link_to "Create article", new_article_path, class: 'btn btn-lg btn-success' %>
    <br><br> 
    <!-- Funcionalidade do rails para iterar os componentes -->
    <!-- a linha abaixo realiza um each.do para cada artigo que foi passado pelo controller -->
    <%= render @articles %>
    <%= render 'pagination' %>
  </div>
  <%= render 'aside' %>
</div>
